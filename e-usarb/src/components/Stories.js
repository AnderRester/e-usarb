import React from "react";
import { View, Image, Linking, TouchableOpacity, Text } from "react-native";

const Stories = () => {
    return <View style={{ backgroundColor: 'blue', paddingTop: 55, alignItems: 'center', justifyContent: 'space-between', flexDirection: 'row', paddingBottom: 10, textAlign: 'center'}}>
        
    </View>
}

export default Stories